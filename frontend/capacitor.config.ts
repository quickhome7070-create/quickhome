import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ghardestiny.app',
  appName: 'GharDestiny',
  server: {
    url: 'https://www.ghardestiny.com',
    cleartext: false
  }
};

export default config;